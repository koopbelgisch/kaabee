extends ../layout.pug
block content
  section.hero.is-primary.is-fullheight.is-bold
    .hero-body
      .container
        h1 KaaBee
        h2 Welkom, wij hebben #{ storeCount } winkels die staat te popelen om je te ontvangen!
        if currentUser
          p
            | Je bent momenteel aangemeld als #{ currentUser.name }.
            a(href="/logout") Afmelden
            | .
        else
          p
            | Je bent niet aangemeld. Ga naar de
            a(href="/login")  aanmeldpagina
            | .
